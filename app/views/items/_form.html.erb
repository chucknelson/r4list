<div id="new-item">
	<%= form_for [@user, @list, @list.items.build] do |f| %>
		<span>
			New Item: <%= f.text_field :name, :autofocus => @item_focus %>
		</span>
		<span>
			<%= f.submit %>
		</span>
	<% end %>
</div>