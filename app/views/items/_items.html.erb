<ul id="list-items" data-ajax-sort-route="<%= user_list_sort_path(@user, @list) %>">
	<% if !@items.empty? %>
		<% @items.each do |item| %>
			<%= render item %>
		<% end %>
	<% end %>

	<%= render 'items/form' %>
</ul>

<!-- <%= render 'items/new_item_placeholder' %> -->